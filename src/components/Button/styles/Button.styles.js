import styled from "styled-components";

export const Btn = styled.div`
    padding: 1rem 2rem;
    background-color: var(--primary-color);
    border: none;
    font-size:1.1rem;
    color: #fff;
    cursor: pointer;
    border-radius: 10px;
    width: fit-content;
  }
`;
